# ==========================================================================
# Gia Phả Việt — biến môi trường
# --------------------------------------------------------------------------
# Để TRỐNG hai biến dưới → app chạy LOCAL-FIRST (IndexedDB + dữ liệu demo),
# KHÔNG cần đăng ký gì. Điền vào → app dùng Supabase (Postgres + Auth + RLS).
# Sao chép file này thành `.env.local` rồi điền giá trị của project Supabase.
# ==========================================================================

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
